# 2026-08-28

- 熟悉 PTNexus 项目结构（只读，未改代码）。
- 关键结论：
  - Go 主线（`go` 分支），后端 `server/`（Gin，入口 `cmd/server/main.go`，装配在 `internal/bootstrap/app.go`，模块名 `github.com/pt-nexus/server`）。
  - 前端 `webui/`（Vue3 + TS + Element Plus，路由在 `src/router/index.ts`，导航菜单硬编码在 `App.vue` 的 el-menu 中）。
  - 页面模块：首页 `/`、流量统计 `/info`、一种多站 `/torrents`、一站多种 `/data`(CrossSeedDataView)、定时发种 `/scheduled-seeding`、自动发种 `/auto-seed`、发种日志 `/publish-logs`、做种检索 `/sites`、设置 `/settings`。
  - 外围：`batch/`、`updater/`、`proxy/`(盒子端)、`desktop/`(Windows 桌面版)、`extension/`(浏览器插件)。
  - `.codex/skills` 下有 worktree-lite、wsl-win-notify、server-standards、commit-message-formatter；AGENTS.md 要求改代码走 worktree-lite 流程。
- 用户首个请求疑似为「隐藏一站多种模块」（打字为"影藏一战多种"），已请求澄清但被跳过，待确认后再动手。
- 用户确认需求：隐藏"一站多种"模块，并明确要求**在当前分支直接修改**（不走 worktree-lite）。
- 已完成：`webui/src/App.vue` 移除桌面导航与移动端抽屉导航中的 `<el-menu-item index="/data">一站多种</el-menu-item>` 两处入口；`pnpm run type-check` 通过。
- 注意：`/data` 路由本身与 CrossSeedDataView 保留，直接输入 URL 仍可访问；`routeTitleMap`、`isRefreshSupportedRoute` 中的 /data 保留不影响。
- 提示：用户未要求提交，未执行任何 git 提交操作。wsl-win-notify 通知发送失败（Toast 与 msg.exe 均不可用，仅记录）。

## 资源信息模块（resource_info）实现完成

用户需求：新增"资源信息"模块（标题/年份/国家/豆瓣ID/IMDbID/TMDbID/海报/简介存库）；抓取时按 豆瓣>IMDb>TMDb 优先级查库，命中则覆盖发布数据（标题/产地/海报/简介）并跳过海报/简介修复；未命中则入库复用；预览页展示资源信息。

后端：
- `server/internal/repository/resource_info.go`：ResourceInfo 模型 + FindByDouban/Imdb/Tmdb + UpsertResourceInfo（任一ID命中即同一资源，补字段更新）+ ListResourceInfos（keyword 分页搜索）。
- `schema_manager.go`：resource_info 建表（sqlite/mysql/pg 三套 DDL）+ columnSpecs + 4 个索引（三个ID+updated_at）。
- `persist/fetch_resource_info.go`：ID 提取正则（douban subject / tt\d+ / themoviedb movie|tv）、FindResourceInfoForDraft（优先级查库）、ApplyResourceInfoToDraft（覆盖 Title/Source/Poster/Body）、SaveResourceInfoFromDraft（未命中入库，年份从标题正则提取、国家取 draft.Source）、AttachResourceInfoToRow（get_db_seed_info 响应附加 resource_info）。
- `fetch_repair_finalize.go` + `fetch_persist_pipeline.go`：抓取修复前查库，命中且库内有海报/简介时通过 ParallelFetchRepairInput.SkipPosterRepair/SkipIntroRepair 跳过对应修复，修复后覆盖草稿；未命中入库。
- `repair/fetch_flow.go`：ParallelFetchRepairInput 新增 Skip 标志，runPosterRepairTask/runIntroRepairTask 早退。
- `handler/resource_info_handler.go` + bootstrap：GET /api/resource_info（keyword/page/page_size）。

前端：
- `views/ResourceInfoView.vue` 新页面（海报缩略图+字段表格+搜索+分页），路由 /resource-info，导航"资源信息"（桌面+移动+routeTitleMap）。
- `CrossSeedStepPublishPreview.vue`：主/副标题与参数之间新增资源信息展示区（数据来自 get_db_seed_info 响应的 resource_info）。
- `crossSeedPanelContext.ts` TorrentData 新增 resource_info? 字段。

验证：go build/test 通过，pnpm type-check 通过。lint 有一个 seedFlow.ts:1010 `hasRestrictedTag` 未使用的历史遗留错误（本次未触碰该文件）。

注意：年份/国家目前来源有限——年份从种子标题正则提取，国家取 draft.Source（源站解析的产地）；无外部元数据源（豆瓣API等），后续可考虑接入。

## 修复：预览时资源信息不入库

用户反馈点「下一步：发布参数预览」后 resource_info 表没有数据。根因：前端点下一步先调 get_db_seed_info，种子之前抓取过时后端直接返回缓存（200），fetch_and_store 不会被调用，而保存钩子原来只在抓取流水线里 → 缓存路径完全不触发入库。

修复：
- `persist/fetch_resource_info.go` 新增 `SaveResourceInfoFromRow`（从 get_db_seed_info 归一化行提取 ID/标题/年份/国家/海报/简介入库，title 缺失回退 name，summary 回退 subtitle）。
- `persist/db_seed_query.go` QueryAndNormalizeSeed：attach 后若 normalized 中无 resource_info 键则调用 SaveResourceInfoFromRow。go build/test 通过。

另外排查发现当前机器 5275（后端）/5173（vite）均无监听进程，服务未在运行；提醒用户需用新代码重启后端才能生效。

## 修复：资源信息页面全部显示 N/A

根因：ResourceInfo 结构体只加了 gorm 标签、没加 json 标签，Gin 序列化输出 Go 默认驼峰键（Title/DoubanID/PosterURL...），前端表格读蛇形键（row.title/row.douban_id/row.poster_url...）全部 undefined → 每列 N/A。已补 json snake_case 标签，go build/test 通过。需重启后端生效。

## 资源信息页面：查看详情 + 复制按钮

按需求给 ResourceInfoView.vue 增加：
- 表格「操作」列「查看」按钮 → 打开详情对话框（海报+标题/年份/国家/三个ID/海报地址/简介/创建时间/更新时间）。
- 表格每个字段单元格内联复制按钮；详情对话框每个字段也有复制按钮。
- copyText 用 navigator.clipboard（HTTPS/localhost 安全上下文），非安全上下文回退到 textarea+execCommand；空值提示，成功 ElMessage。
- 导入 @element-plus/icons-vue 的 CopyDocument / View 图标。
前端 type-check 通过。仅前端改动，无需改后端/重启后端（重启前端 dev 即生效）。

## 资源信息详情增强：复制全部 + 海报跳转打开

用户确认追加两项：详情对话框顶部加「复制全部」按钮（copyAll 把标题/年份/国家/三个ID/海报地址/简介/创建时间/更新时间拼成多行文本复制）；海报地址字段新增「跳转打开」按钮（openPoster，window.open 新标签，空值提示）。导入 Link 图标。前端 type-check 通过。

## 修复：海报地址 BBCode 导致展示失败

根因：海报字段存成 BBCode 形式 `[img]https://...jpg[/img]`，`<el-image :src>` 拿到整段文本而非纯 URL，图片无法加载。

修复（双保险）：
- 后端 `repository/resource_info.go`：新增 `normalizePosterURL`（正则剥离 `[img=..]..[/img]`，取内部 URL，回退原文本），在 写入（UpsertResourceInfo 入库前）与 读取（findResourceInfoByColumn、ListResourceInfos 扫描后）两处均清洗 → 已存的脏数据无需迁移即可正常展示。
- 前端 `ResourceInfoView.vue`：新增 `cleanPoster` 兜底函数，列表缩略图、详情大图、跳转打开均先剥离 BBCode。
- `getProxyImageUrl` 经核实首次返回原 URL、仅图片 onerror 才切换代理域名，与本次无关；本次核心修复是 URL 格式。
go build/test 与前端 type-check 均通过。需重启后端生效。

## 修复：资源信息查库时机错误（应先获取数据再查库）

用户指出正确时序：必须先「获取种子数据」（修复流程会补全 豆瓣/IMDb/TMDb ID），拿到完整 ID 后才能去 resource_info 查库并覆盖海报/简介；之前的实现在 RunParallelFetchRepairs 之前就查库，那时 draft 只有源站页面原始 ID，PTGen 补全的 ID 还没拿到 → 纯 IMDb 等种子查不到、覆盖不到。

证据：`seed_draft.go:143-145` ApplyRepairResult 会把 PTGen 补全后的 IMDbLink/DoubanLink/TMDbLink 写回 draft；旧代码 fetch_repair_finalize.go:72 在修复前查库。

修复：
- `fetch_repair_finalize.go`：把 `FindResourceInfoForDraft` + `ApplyResourceInfoToDraft`/`SaveResourceInfoFromDraft` 从「修复前」移到 `ApplyRepairResult` 之后（即先抓数据再查库）；命中则覆盖海报+简介（正文）（ApplyResourceInfoToDraft 同时覆盖 Title/Source），未命中则保存本次抓取结果（含 PTGen 补全的 ID 与海报/简介）。删除早期 skip 逻辑。
- `fetch_flow.go`：ParallelFetchRepairInput 移除 SkipPosterRepair/SkipIntroRepair 字段，runPosterRepairTask/runIntroRepairTask 删除对应早退块（不再跳过修复，保证先获取完整数据）。
- `fetch_resource_info.go`：删除已无调用的 ResourceInfoRepairSkips。
go build/test 通过。需重启后端生效。

## 资源信息：新增视频截图存储与复用

用户需求：资源信息把「视频截图」也存库；在视频截图环节按 当前种子 douban_id→imdb_id→tmdb_id 命中资源信息库时，复用库内已存截图。

后端：
- `repository/resource_info.go`：ResourceInfo 新增 `Screenshots` 字段（gorm column + json snake_case）；UpsertResourceInfo 在 trim 与 merge fill 中纳入 screenshots（任一ID命中即补字段）。
- `schema_manager.go`：resource_info 三套 DDL（mysql LONGTEXT / pg TEXT / sqlite TEXT）加 screenshots 列；columnSpecs 加 screenshots 以便存量库自动 ALTER ADD COLUMN。
- `persist/fetch_resource_info.go`：ApplyResourceInfoToDraft 命中且库内有 screenshots 时覆盖 draft.Screenshots；SaveResourceInfoFromDraft / SaveResourceInfoFromRow 保存 draft/normalized 的 screenshots；AttachResourceInfoToRow 的 resource_info map 加 screenshots 键。
- `persist/fetch_repair_finalize.go`：命中但库内截图为空、而本次抓取有截图时，回补 UpsertResourceInfo（仅填 screenshots），避免重复生成不入库存量。

前端：
- `crossSeedPanelContext.ts` TorrentData.resource_info 加 screenshots。
- `CrossSeedStepPublishPreview.vue`：资源信息展示区新增「视频截图」缩略图画廊（解析 [img]url[/img]）。
- `views/ResourceInfoView.vue`：详情对话框新增「视频截图」画廊 + 「复制截图」按钮（不纳入 copyAll，避免巨型剪贴板）。

验证：go build ./... 通过；webui pnpm run type-check 通过。需重启后端生效（服务当前未运行）。

