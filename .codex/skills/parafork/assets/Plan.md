# 计划（Plan）

> 仅当 `settings/config.toml` 的 `custom.autoplan=true` 时，`parafork init --new`（或无参默认入口触发的 `init --new`）才会创建本文件，且 `parafork check` 会检查其结构。
> 注意：`## Milestones` 和 `## Tasks` 两个标题必须保留为固定字符串（供 `check` 机械判定）。

## Milestones
- [ ] M1: PARAFORK_TBD

## Tasks
- [ ] T1: PARAFORK_TBD（milestone: M1）— 验收：PARAFORK_TBD
